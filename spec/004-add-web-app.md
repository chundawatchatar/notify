# Web app

This is the main frontend of the application where users interact with Notify.

## Implementation

- Use TanStack Start for the web application.
- Do not implement API logic; this app is frontend-only for now.
- Use the shared UI library and centralized Tailwind/shadcn styling from the workspace.
- Configure Vitest for testing without adding test cases yet.

## Status

- Implemented `@notify/web` as a TanStack Start app under `apps/web`.
- Added Vitest configuration for the web app.
- Reused `@notify/ui` components and `@notify/styles` shared Tailwind/shadcn styles.
