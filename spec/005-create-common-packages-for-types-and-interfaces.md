# Common Package For Types And Interfaces

Create a common package under the `packages` folder with a basic TypeScript
setup. Shared packages should use TypeScript.

## Status

- Implemented `@notify/common` under `packages/common`.
- Added TypeScript build and typecheck configuration.
- Added shared notification domain types and interfaces.
