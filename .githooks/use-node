export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"

if [ ! -s "$NVM_DIR/nvm.sh" ]; then
  echo "nvm is required for Git hooks. Install nvm or set NVM_DIR." >&2
  exit 1
fi

. "$NVM_DIR/nvm.sh"
nvm use --silent >/dev/null
