# Notify OpenAPI

Framework-free Elixir OpenAPI schema modules shared by the Phoenix API contract.

This library owns reusable schema definitions. The API app owns routes,
controllers, operation specs, and OpenAPI JSON generation.
